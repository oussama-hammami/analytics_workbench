# Analytics Workbench — Envato CodeCanyon Sales Copy

> Paste the content below directly into the CodeCanyon "Description" field.
> CodeCanyon renders `[b]`, `[i]`, `[list]`, `[*]` BB-code tags in item descriptions.
> The Markdown version follows for use on your own landing page / readme preview.

---

## ── CODECANYON BB-CODE VERSION ──────────────────────────────────────────

[b]🤖 Analytics Workbench — Self-Hosted AI & Machine Learning Platform[/b]

Train models. Explore data. Own your infrastructure. No cloud bills. No data leaks.

[b]Analytics Workbench[/b] is a production-ready, browser-based AI/ML platform you deploy on your own server. Upload a dataset, pick a model, click Train — and get results in seconds. With [b]16 pre-built models[/b] across scikit-learn, Keras, and PyTorch, a full data preprocessing pipeline, rich visualizations, and a polished admin control panel, it is everything a data team needs — packaged as a single Python app that installs in under a minute.

─────────────────────────────────────────────────

[b]⚡ Key Features[/b]

[list]
[*] [b]16 Ready-to-Train ML Models[/b] — Linear & Logistic Regression, Random Forest, Extra Trees, KNN, SVM (scikit-learn) + MLP, CNN, LSTM, GRU, RNN in both Keras/TensorFlow and PyTorch. CPU-optimised — no GPU required.
[*] [b]End-to-End Data Pipeline[/b] — Upload CSV or Excel, handle missing values, encode categoricals, scale features, split into train/test sets, and feed straight into any model. All in one UI.
[*] [b]Outlier Detection Suite[/b] — Three methods out of the box: IQR, Z-score, and Isolation Forest. Visualise affected rows and remove them with a single click.
[*] [b]Rich Visualization Layer[/b] — Distribution histograms, correlation heatmaps, scatter-matrix pair plots, and box plots — all interactive, powered by Plotly.
[*] [b]Live Prediction Export[/b] — Run inference on new data files or enter values manually. Download results as CSV.
[*] [b]1-Minute Installer[/b] — One shell script (`setup.sh`) handles Python venv creation, package installation, database bootstrapping, default admin seeding, and app launch. No Docker. No Kubernetes. No configuration files to hand-edit.
[*] [b]Zero-Config SQLite Database[/b] — No MySQL, PostgreSQL, or external database server needed. The database creates itself automatically on first boot.
[*] [b]Premium Admin Control Panel[/b] — Role-based access control (Superadmin / Admin / Viewer), full User CRUD with inline editing, active/inactive toggles, and password reset — all from a clean dark-themed UI.
[*] [b]Encrypted API Key Vault[/b] — Store credentials for OpenAI, Stripe, AWS, or any external service. Keys are encrypted with AES-128 (Fernet) derived from your own secret key — never stored in plaintext.
[*] [b]Analytics Dashboard[/b] — KPI cards with delta indicators, 30-day traffic line chart, user role doughnut chart, and top module usage bar chart. Skeleton loaders, empty states, and a live/sample data badge included.
[*] [b]Site Settings Manager[/b] — Change the platform name, upload a logo, edit footer text, set a contact email, and toggle maintenance mode — without touching a single line of code.
[*] [b]100% Self-Hosted & Private[/b] — Your data never leaves your server. No telemetry. No third-party analytics. Full source code included.
[*] [b]Developer-Friendly Codebase[/b] — Clean modular architecture: separate packages for core pipeline, model implementations, admin module, and utilities. Easy to extend or white-label.
[/list]

─────────────────────────────────────────────────

[b]📦 What's Included[/b]

[list]
[*] Full Python source code (Streamlit, scikit-learn, Keras, PyTorch)
[*] Admin module: auth, RBAC, dashboard, user management, API key vault, settings
[*] `setup.sh` one-command installer with `--run-only` and `--reset-admin` flags
[*] `.env.example` configuration template
[*] Sample datasets for immediate testing
[*] Complete technical documentation (PDF + Markdown)
[*] `systemd` service configuration for production deployment
[/list]

─────────────────────────────────────────────────

[b]🏆 Why Choose Analytics Workbench?[/b]

[b]Privacy first, always.[/b]
Cloud ML platforms charge per API call, per seat, per prediction — and they read your data. Analytics Workbench runs entirely on your own infrastructure. Your training data, your models, your business insights: none of it touches a third-party server.

[b]No devops degree required.[/b]
Most self-hosted ML tools demand Docker Compose stacks, cloud credentials, or a dedicated database server before you can train a single model. Analytics Workbench needs Python 3.9 and one command. That's it.

[b]16 models. Three frameworks. One interface.[/b]
Switching between scikit-learn, Keras, and PyTorch normally means three different codebases, three different training loops, three different evaluation scripts. Here, you switch with a tab click. Same dataset. Same workflow. Same export format.

[b]Built for teams, not just solo analysts.[/b]
The admin panel handles multi-user access gracefully. Invite a Viewer who can explore the ML tools but cannot touch admin settings. Promote them to Admin when they're ready. Revoke access in one click. No hacking config files.

[b]Code you can actually read.[/b]
Every model, every preprocessing step, and every admin feature lives in its own clearly named file. The folder structure mirrors the UI structure. Customising, extending, or white-labelling the platform takes minutes, not days.

─────────────────────────────────────────────────

[b]🖥️ Server Requirements[/b]

[list]
[*] Python 3.9+ (3.10 / 3.11 recommended)
[*] 4 GB RAM minimum (8 GB recommended for deep learning)
[*] 5 GB free disk space
[*] Ubuntu 20.04 / Debian 11 or later (Windows WSL2 also supported)
[*] Port 8501 open (configurable)
[/list]

No MySQL. No Redis. No message broker. SQLite is bundled with Python.

─────────────────────────────────────────────────

[b]🔐 Security Highlights[/b]

[list]
[*] Passwords hashed with bcrypt (cost factor 12) — never stored in plaintext
[*] API keys encrypted with Fernet AES-128-CBC derived from your `APP_SECRET_KEY`
[*] Role-based access control enforced server-side on every page render
[*] Maintenance mode lockout for non-admin users during updates
[*] Masked key previews — only the first 4 and last 4 characters of stored API keys are visible
[/list]

─────────────────────────────────────────────────

[b]🛠️ Support & Suggestions[/b]

We stand behind every line of code in Analytics Workbench and are committed to making your experience smooth from day one.

[b]Before you contact support, please:[/b]
[list]
[*] Read the included [b]DOCUMENTATION.md[/b] — it covers installation, configuration, all features, and a detailed troubleshooting guide for the most common issues.
[*] Check the [b]FAQ tab[/b] on this listing — we update it regularly based on buyer questions.
[/list]

[b]We offer:[/b]
[list]
[*] [b]6 months of included support[/b] (extendable via Envato) covering installation help, bug fixes, and configuration questions
[*] Response within [b]24–48 hours[/b] on business days via the Envato comments system
[*] [b]Bug fixes[/b] delivered as free updates — if something doesn't work as documented, we fix it
[/list]

[b]We'd love to hear from you:[/b]
[list]
[*] Found a bug? Open a comment with your Python version, OS, and the exact error message.
[*] Have a feature idea? Drop it in the comments — popular requests make it into future versions.
[*] Leaving a rating? We read every review and use them to guide our roadmap.
[/list]

[b]Out of scope for support:[/b] Custom feature development, server administration beyond the documented setup steps, and modifications to the core source code. For custom work, contact us directly to discuss a quote.

─────────────────────────────────────────────────

[b]⭐ If Analytics Workbench saves you time, please leave a rating — it helps other buyers find the product and keeps development active. Thank you![/b]

---

## ── MARKDOWN VERSION (Landing Page / GitHub / ReadMe) ───────────────────────

# 🤖 Analytics Workbench

### Self-Hosted AI & Machine Learning Platform — Train models. Explore data. Own your infrastructure.

> No cloud bills. No data leaks. No configuration headaches.

**Analytics Workbench** is a production-ready, browser-based AI/ML platform you deploy on your own server. Upload a dataset, pick a model, click Train — and get results in seconds. With **16 pre-built models** across scikit-learn, Keras, and PyTorch, a complete preprocessing pipeline, interactive visualizations, and a polished admin control panel, it's everything a data team needs — packaged as a single Python application that installs in under a minute.

---

## ⚡ Key Features

- **16 Ready-to-Train ML Models** — Linear & Logistic Regression, Random Forest, Extra Trees, KNN, SVM (scikit-learn) + MLP, CNN, LSTM, GRU, RNN in both Keras/TensorFlow and PyTorch. CPU-optimised — no GPU required.
- **End-to-End Data Pipeline** — Upload CSV or Excel, handle missing values, encode categoricals, scale features, split train/test sets, and feed straight into any model. All in one UI.
- **Outlier Detection Suite** — IQR, Z-score, and Isolation Forest. Visualise affected rows and remove them with one click.
- **Rich Visualization Layer** — Interactive distribution histograms, correlation heatmaps, pair plots, and box plots powered by Plotly.
- **Live Prediction Export** — Run inference on new files or manual input. Download results as CSV.
- **1-Minute Installer** — One shell script (`setup.sh`) does everything: venv, packages, database, admin account, launch.
- **Zero-Config SQLite Database** — No MySQL, PostgreSQL, or external database server required.
- **Premium Admin Control Panel** — RBAC (Superadmin / Admin / Viewer), full User CRUD, active/inactive toggles, password reset.
- **Encrypted API Key Vault** — AES-128 Fernet encryption derived from your own secret key. No plaintext storage.
- **Analytics Dashboard** — KPI cards, 30-day traffic chart, role distribution, module usage — with skeleton loaders and empty states.
- **Site Settings Manager** — Platform name, logo upload, footer, maintenance mode — no code edits needed.
- **100% Self-Hosted & Private** — Your data never leaves your server. Full source code included.

---

## 🏆 Why Choose Analytics Workbench?

**Privacy first, always.**
Your training data, your models, your business insights — none of it touches a third-party server. No per-seat fees. No API call charges.

**No devops degree required.**
Python 3.9 and one command. No Docker. No Kubernetes. No external database to configure.

**16 models. Three frameworks. One interface.**
Switch between scikit-learn, Keras, and PyTorch with a tab click — same dataset, same workflow, same export format.

**Built for teams.**
Invite Viewers, promote Admins, revoke access instantly. Multi-user RBAC without hacking config files.

**Code you can actually read.**
Clean modular architecture — each feature lives in its own file. Easy to extend, customise, or white-label.

---

## 📦 What's Included

| Item | Details |
|---|---|
| Full source code | Python, Streamlit, scikit-learn, Keras, PyTorch |
| Admin module | Auth, RBAC, dashboard, users, API vault, settings |
| Installer | `setup.sh` with `--run-only` and `--reset-admin` flags |
| Configuration | `.env.example` template |
| Sample data | Ready-to-use test datasets |
| Documentation | Complete technical manual (Markdown + PDF) |
| Production config | `systemd` service unit for background deployment |

---

## 🔐 Security Highlights

- Passwords hashed with **bcrypt** (cost factor 12)
- API keys encrypted with **Fernet AES-128-CBC** keyed from your `APP_SECRET_KEY`
- **Server-side RBAC** enforced on every page render — no client-side-only guards
- **Maintenance mode** lockout protects users during updates
- **Masked key previews** — only first 4 and last 4 characters of any stored key are ever displayed

---

## 🛠️ Support & Suggestions

We stand behind every line of code and are committed to a smooth experience from day one.

**Included support covers:**
- Installation help and configuration questions
- Bug reports with reproducible steps
- 24–48 hour response time on business days (Envato comments)
- 6 months of support included, extendable via Envato

**To get the fastest help:**
1. Read **DOCUMENTATION.md** — it covers every common issue with exact fix commands.
2. Include your Python version, OS, and the full error message in your report.

**Feature suggestions are welcome** — popular requests are prioritised in future updates.

> ⭐ If Analytics Workbench saves you time, a rating on CodeCanyon keeps development active and helps other buyers find the product. Thank you!
